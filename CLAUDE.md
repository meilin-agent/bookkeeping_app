# CLAUDE.md — 记账项目指南

> 本项目由 Claude Code 与用户（非程序员）协作开发。
> **本文件是项目的「宪法」，每次会话开始时自动加载，必须全程遵守。**

---

## 一、项目概况

- **产品名称**：记账（桌面记账软件；曾用名：黑马记账，2026-08-20 用户要求改名）
- **运行平台**：Windows 电脑 + Mac 电脑
- **核心功能**：记录每笔收支（人民币、精确到分），2 级分类（一级大类 + 二级小类），流水列表，本月汇总，分类管理
- **技术栈（已确认）**：Tauri 2 + Vue 3 + TypeScript + SQLite（数据全部存本机，不联网）
- **产品设计**：见 `产品设计文档.md`（所有功能与界面以此为准）

## 二、协作铁律（最高优先级，任何情况下不得违反）

用户不懂编程，且明确要求：**项目期间任何关于技术方面的事情，用户无法提供具体要求，必须由 Claude 列方案并解释，由用户决定。**

1. **大白话沟通**：所有技术解释必须用通俗语言 + 生活化比喻。必须使用术语时，先解释清楚再使用。

2. **技术决策流程（铁律）**：
   - 任何技术方面的事情——大到架构选型（用什么技术、怎么存数据），小到引入一个依赖库、选一个 UI 组件、定一个数据结构——**都不得擅自决定**
   - Claude 必须先列出 **2~4 个候选方案**，每个方案用大白话说明：是什么、优点、缺点、适用场景、是否推荐
   - 用户拍板后，才能动手实施
   - 用户没有表态的方案，一律视为未决定，不得先斩后奏

3. **决策记录表**：每落地一个技术决策，立即把「候选方案 + 用户选择 + 理由」追加到本文件第四节（见下方表格）。

4. **产品功能变更同样需要确认**：新增/修改/删除功能、改界面、改分类，也要先给方案或说明理由，经用户同意后执行。

5. **小步迭代交付**：
   - 每次只做一小步，做**可运行的版本**再交付
   - 每次改动后用大白话总结：「做了什么、现在能干什么」
   - 关键节点（如首次弹出窗口、记账功能完成）停下来让用户验收
   - 不得一口气把 V1 全部做完才给用户看

## 三、开发环境备忘

- 项目根目录：`d:\Study Room\vs code\记账项目文件\黑马记账APP\`（本文件夹；2026-08-20 从原位置迁移而来）
- 前端代码在 `src/`，桌面外壳在 `src-tauri/`
- 常用命令：
  - 启动开发版：`npm run tauri dev`
  - 构建安装包：`npm run tauri build`
- 数据文件：SQLite 数据库，位于本机应用数据目录（不放进项目文件夹，避免代码上传时泄露账目）

**待办提醒（用户提出的需求，勿忘）**：
- 用户希望：**项目完成后（打包出正式安装包、用户验收无误后），在获得用户允许的情况下，卸载开发工具链（Rust 约1GB + VS Build Tools 约2~6GB），释放硬盘空间**
- 临时安装器文件（`..\下载的安装程序\` 下的 exe，已从原临时目录迁入）可随时删除
- 卸载前必须再次征得用户同意，并提醒：卸载后无法再修改 APP，如需开发 V2/V3 需重新安装工具（约 20~30 分钟）

## 四、技术决策记录表

每行一条决策，按时间顺序追加。**任何技术决策落地后必须更新此表。**

| 日期 | 决策事项 | 候选方案 | 用户选择 | 理由 / 备注 |
|---|---|---|---|---|
| 2026-08-20 | 整体技术栈 | A. Tauri（轻量、约10MB、省内存，工具链较重）/ B. Electron（成熟主流、100MB+、占内存多）/ C. Flutter（可复用做手机版，桌面生态较弱）/ D. 本地网页（最简单，无APP体验） | **A. Tauri** | 记账软件常驻运行，轻量省内存最重要 |
| 2026-08-20 | V1 功能范围 | 精简版（记账核心）/ 标准版（+统计图表）/ 完整版（+预算与导出） | **精简版** | 先跑通核心，再逐步扩展 |
| 2026-08-20 | 是否记录收入 | 只记支出 / 支出+收入 | **支出 + 收入** | 可看每月结余，与主流记账APP一致 |
| 2026-08-20 | 界面技术（前端框架） | Vue 3（中文资料最丰富）/ React（全球最大）/ Svelte（最新但小众） | **Vue 3** | 中文资料多、开发效率高 |
| 2026-08-20 | 数据存储方式 | SQLite 数据库（可靠、统计导出方便）/ 纯 JSON 文件（最简单、数据多了变慢） | **SQLite 数据库** | 记账软件标准做法，为 V2 统计打基础 |
| 2026-08-20 | 界面风格 | 明亮清爽 / 深色 / 跟随系统 | **明亮清爽** | 经典记账APP风格，白底清晰 |
| 2026-08-20 | 页面切换方式 | 简单切换（底部按钮直接切换，零依赖）/ 专业路由库（vue-router） | **简单切换** | 记账APP页面少，无需复杂路由 |
| 2026-08-20 | 分类图标 | emoji 表情（系统自带、免费、双平台可用）/ 专业图标库（需联网下载资源） | **emoji 表情** | 零依赖、双平台显示一致 |
| 2026-08-20 | 界面组件 | 全部手写样式（零依赖、体积最小）/ UI 组件库（Vant，开发快但增体积） | **全部手写** | 符合 Tauri 轻量定位，样式完全可控 |
| 2026-08-20 | 删除有记录的分类 | 禁止删除（需先处理记录，不丢数据）/ 连同记录一起删 / 分类可删、记录保留 | **禁止删除有记录的分类** | 最安全，不丢任何账目数据 |

## 五、文档索引

| 文档 | 位置 | 说明 |
|---|---|---|
| 产品设计文档 | `产品设计文档.md` | 功能、分类体系、界面设计、版本规划 |
| 本指南 | `CLAUDE.md` | 协作铁律 + 决策记录表（本文件） |

---

*本文件由 Claude 与用户共同维护。铁律部分任何修改需用户确认。*
